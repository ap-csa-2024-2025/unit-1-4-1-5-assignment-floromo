public class Problem0
{
  public static void main(String[] args)
  {
    double y = 1;
    System.out.println(y);

    int x = 1.5;
    System.out.println(x);

    // Can store an int into a double but not a double into an int because doubles are bigger than ints (in memory). For example, you can put people into a car but not a car into people. 
  }
}
